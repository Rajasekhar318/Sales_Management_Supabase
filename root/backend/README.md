# TruEstate Backend (Express + MySQL)

## Setup
1. Install dependencies:
   npm install

2. Create .env with DB credentials (see root instructions).

3. Create database in MySQL:
   CREATE DATABASE truestate;

4. Run seed:
   npm run seed

5. Start dev server:
   npm run dev
